<header>
  ranking
</header>

<% @level.each do |level| %>
  <div class="level-wrapper">
    <div class="radius-btn-wrapper">
      <%= link_to "level#{level.id}", ranking_en_level_path(level), class: "nav-link btn radius-btn btn-blue" %>
    </div>
  </div>
<% end %>