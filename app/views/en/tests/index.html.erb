<header>
  test
</header>

<div class="level-container">
  <% @level.each do |level| %>
    <div class="level-wrapper">
      <div class="level-name">
        <%= level.name %>
      </div>
      <div class="radius-btn-wrapper">
        <%= link_to "start", question_level_path(level), class: "nav-link btn radius-btn btn-blue" %>
      </div>
    </div>
  <% end %>
</div>